# Dependencias do projeto Peter_Copy_Ads

# Leitura/escrita de planilhas
openpyxl
xlrd

# HTTP e uploads
requests

# Variaveis de ambiente
python-dotenv

# Servidor web (frontend)
fastapi
uvicorn[standard]
python-multipart

# Deteccao de quadros em imagens (filtro pos-Etsy)
# - opencv-python-headless: heuristica de contornos (gratuita, padrao)
# - google-genai:           Gemini 2.5 Flash Vision para filtro de imagens
opencv-python-headless>=4.8
numpy
google-genai>=1.0

# LLM para traducao de titulos Etsy -> PT (GPT-4o-mini, ~US$0.0001/chamada)
# Migrado de Gemini Flash em 28/04/2026 por estabilidade superior (sem 503).
openai

# Crop quadrado (1:1) das 4 imagens do produto antes do upload pro ImgBB —
# Shopee exige imagens quadradas pro card de listagem (CTR algoritmico).
Pillow

# Banco de SKUs em uso (Supabase via Marketplace Vercel). Substitui o
# skus_em_uso.json local que era volatil no /tmp da Lambda Vercel. Caminho
# pro ERP futuro: mesmo banco recebe tabelas adicionais (produtos, vendas).
# Sem SDK — usamos PostgREST direto via `requests` (ja na dep acima).
# Motivo: supabase-py puxa pyiceberg que requer Microsoft C++ Build Tools
# no Windows, complica o setup local sem trazer ganho real pro nosso caso
# (CRUD simples de uma unica tabela).
